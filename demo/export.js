import {cp,mkdir,rm,writeFile} from 'node:fs/promises';
import {companies,company,fundamentals} from './data.js';
// Preserve screenshots while replacing just the reproducible site output.
await mkdir('docs',{recursive:true});await rm('docs/assets',{recursive:true,force:true});
await cp('dist','docs',{recursive:true});await writeFile('docs/.nojekyll','');
const rows=companies.map(c=>{const f=fundamentals(c.ticker);return `<tr><td>${c.ticker}</td><td>${company(c.ticker).name}</td><td>$${(f.values.revenue/1e9).toFixed(1)}B</td><td>${(f.values.fcfMargin*100).toFixed(1)}%</td></tr>`;}).join('');
await writeFile('docs/demo-data.html',`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Generated demo data</title><style>body{font:16px system-ui;max-width:800px;margin:60px auto;padding:24px;color:#24344b}td,th{padding:12px;text-align:left;border-bottom:1px solid #ddd}table{border-collapse:collapse}a{color:#315fb5}</style><h1>Generated demonstration data</h1><p>All companies, financial observations, simulated filings, executive profiles and research narratives in this website are fictional examples. No live provider, WRDS, FRED, SEC or AI service is connected.</p><table><tr><th>Label</th><th>Sample company</th><th>Revenue</th><th>FCF margin</th></tr>${rows}</table><p>DCF values, sensitivity grids, returns and correlations are calculated from generated inputs. Refresh resets the same reproducible scenario.</p><p><a href="./">Return to dashboard</a> · <a href="https://github.com/shaylazheng/investment-analyst-dashboard-public/blob/main/demo/data.js">View generator source</a></p></html>`);
